# ------------------------------------------------------------------
# CONFIGURACIÓN DE BASE DE DATOS (CONVEX)
# Se generan automáticamente al correr `npx convex dev`, 
# pero es bueno tenerlas documentadas aquí.
# ------------------------------------------------------------------
CONVEX_DEPLOYMENT=dev:tu-proyecto-convex-123 # ID de despliegue en Convex
NEXT_PUBLIC_CONVEX_URL=https://tu-proyecto-convex-123.convex.cloud

# ------------------------------------------------------------------
# CONFIGURACIÓN DE ORQUESTADOR (n8n - Docker Compose)
# ------------------------------------------------------------------
# Usuario y contraseña para entrar al editor de n8n local
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=tu_password_seguro

# Clave aleatoria para cifrar las credenciales dentro de n8n (¡No la pierdas!)
# Puedes generar una con: openssl rand -hex 16
N8N_ENCRYPTION_KEY=generar_string_random_aqui

# URL donde correrá n8n (para los webhooks)
WEBHOOK_URL=http://localhost:5678/
N8N_PORT=5678

# Zona horaria para los cron jobs y fechas
GENERIC_TIMEZONE=America/Argentina/Buenos_Aires

# ------------------------------------------------------------------
# API KEYS PARA SERVICIOS EXTERNOS
# (Estas se pueden inyectar en n8n o cargar manualmente en el UI de n8n /credentials)
# ------------------------------------------------------------------

# Inteligencia Artificial (Cerebro - NLU)
OPENAI_API_KEY=sk-...
# o ANTHROPIC_API_KEY=sk-ant-...

# Generación de Imágenes (Replicate / DALL-E)
REPLICATE_API_TOKEN=r8_...

# Envío de Correos a Terceros (SMTP)
# Ejemplo con Gmail (usar App Password) o SendGrid
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=tu_email@gmail.com
SMTP_PASS=tu_app_password

# Generación de PDF (Si usas servicio externo como APITemplate.io)
# Si usas Gotenberg (Docker), no necesitas API Key, solo la URL local
# PDF_API_KEY=...
