{
  "name": "ooh-agent",
  "version": "0.1.0",
  "description": "Agente comercial automatizado para publicidad exterior (OOH)",
  "private": true,
  "scripts": {
    "dev": "npx convex dev",
    "deploy": "npx convex deploy",
    "lint": "eslint . --ext .ts,.tsx",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "convex": "^1.17.4"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "eslint": "^9.17.0"
  }
}
